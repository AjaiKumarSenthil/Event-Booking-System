replicaCount: 1

image:
  # Registry/namespace comes from global.image.registry (set once for all services).
  repository: bookmyshow-inventory
  tag: latest
  pullPolicy: IfNotPresent

imagePullSecrets: []

serviceAccount:
  create: true

service:
  enabled: true
  type: ClusterIP
  port: 8081

healthPath: /inventory/v1/actuator/health

probes:
  liveness:
    initialDelaySeconds: 40
    periodSeconds: 15
    failureThreshold: 6
  readiness:
    initialDelaySeconds: 20
    periodSeconds: 10
    failureThreshold: 6

resources:
  requests:
    cpu: 100m
    memory: 384Mi
  limits:
    memory: 768Mi

database:
  enabled: true
  host: infra-postgresql
  port: 5432
  name: bookmyshow
  username: catalog
  password: catalog

redis:
  enabled: true
  host: infra-redis-master
  port: 6379

kafka:
  enabled: true
  bootstrapServers: infra-kafka:9092

extraEnv: {}

secrets:
  existingSecret: ""
  extra: {}

jwt:
  enabled: false
  mountPath: /etc/jwt
  keyFileName: auth-private.pem
  privateKey: ""
  existingSecret: ""

ingress:
  enabled: false
  className: nginx
  host: bookmyshow.local
  path: /
  pathType: Prefix
  annotations: {}
  tls: []
